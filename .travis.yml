language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install . --user
# tests should run from the tests directory
before_script: cd sciope/tests
# command to run tests
script: py.test
